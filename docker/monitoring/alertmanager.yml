global:
  resolve_timeout: 5m

route:
  group_by: [alertname, dag_id]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
  receiver: default

receivers:
  - name: default
    webhook_configs:
      - url: http://grafana:3000/api/alertmanager/grafana
        send_resolved: true
