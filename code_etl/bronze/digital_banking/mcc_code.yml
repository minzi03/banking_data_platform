# Bronze Ingestion: mcc_code (dimension — full snapshot)
source:
  type: postgresql
  schema: digital_banking
  fetchsize: 10000

target:
  catalog: lakehouse
  schema: bronze
  table: core_mcc_code

load:
  strategy: full_snapshot

sql: |
  SELECT
    mcc_code,
    description,
    category_group,
    is_high_risk,
    last_updated
  FROM digital_banking.mcc_code
