# =============================================================================
# dbt Packages — Banking Data Platform
# =============================================================================

packages:
  # Core packages
  - package: dbt-labs/dbt_utils
    version: ">=1.1.0"

  - package: dbt-labs/codegen
    version: ">=0.12.0"

  # Date/time utilities
  - package: calogica/dbt_date
    version: ">=0.9.0"
