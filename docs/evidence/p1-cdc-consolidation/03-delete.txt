# Evidence: P1.2 — DELETE Handling

## Test: INSERT → DELETE customer 999999

### Step 1: INSERT test customer
```sql
-- PostgreSQL
INSERT INTO core_banking.customer (customer_id, cccd, full_name, gender, date_of_birth,
  phone, email, address, city, district, branch_code, customer_segment, kyc_status,
  register_date, is_active)
VALUES (999999, '999999999999', 'DELETE TEST USER', 'M', '1990-01-01',
  '0900000000', 'delete.test@example.com', '123 Test St', 'Ho Chi Minh', 'Quan 1',
  'BR001', 'RETAIL', 'VERIFIED', '2024-01-01', 1);
-- Result: INSERT 0 1
```

### Step 2: After INSERT consolidation — Silver has row
```sql
SELECT customer_id, full_name, email, __cdc_operation
FROM lakehouse.silver.dim_customer_current
WHERE customer_id = 999999;

-- Result:
-- customer_id | full_name        | email                 | __cdc_operation
-- 999999      | DELETE TEST USER | delete.test@example.com | INSERT
```

### Step 3: DELETE from PostgreSQL
```sql
DELETE FROM core_banking.customer WHERE customer_id = 999999;
-- Result: DELETE 1
```

### Step 4: Bronze CDC captures DELETE (append-only)
```sql
SELECT __cdc_operation, customer_id, __cdc_timestamp_ms
FROM lakehouse.bronze.core_customer_cdc
WHERE customer_id = 999999
ORDER BY __cdc_timestamp_ms;

-- Result:
-- __cdc_operation | customer_id | __cdc_timestamp_ms
-- INSERT          | 999999      | 1786193017399
-- DELETE          | 999999      | 1786193213758
```

### Step 5: After DELETE consolidation — Silver row removed
```sql
SELECT *
FROM lakehouse.silver.dim_customer_current
WHERE customer_id = 999999;

-- Result: 0 rows (empty)
```

### Step 6: Row count restored
```sql
SELECT COUNT(*) AS rows, COUNT(DISTINCT customer_id) AS distinct_customers
FROM lakehouse.silver.dim_customer_current;

-- Result:
-- rows = 10,000
-- distinct_customers = 10,000
```

## Verification
- Bronze remains append-only (INSERT + DELETE both preserved) ✅
- Silver correctly removes deleted row ✅
- rows = distinct_customers (no orphans) ✅
