# Evidence: P1.1 — Initial Load (Customer + Account)

## Customer Initial Load

```sql
-- Silver dim_customer_current after initial consolidation
SELECT COUNT(*) AS rows, COUNT(DISTINCT customer_id) AS distinct_customers
FROM lakehouse.silver.dim_customer_current;

-- Result:
-- rows = 10,000
-- distinct_customers = 10,000
```

```sql
-- Watermark created
SELECT * FROM lakehouse.meta.cdc_watermark WHERE table_name = 'dim_customer_current';

-- Result:
-- table_name | last_cdc_timestamp_ms | last_spark_batch_id | last_processed_at
-- dim_customer_current | 1786175929765 | 1 | 2026-08-08 08:03:21.927800 UTC
```

## Account Initial Load

```sql
-- Silver dim_account_current after initial consolidation
SELECT COUNT(*) AS rows, COUNT(DISTINCT account_id) AS distinct_accounts
FROM lakehouse.silver.dim_account_current;

-- Result:
-- rows = 30,000
-- distinct_accounts = 30,000
```

```sql
-- Watermark created
SELECT * FROM lakehouse.meta.cdc_watermark WHERE table_name = 'dim_account_current';

-- Result:
-- table_name | last_cdc_timestamp_ms | last_spark_batch_id | last_processed_at
-- dim_account_current | 1785997836403 | 0 | 2026-08-08 12:52:28.303124 UTC
```

## Verification
- rows = distinct_customers ✅ (no duplicates)
- rows = distinct_accounts ✅ (no duplicates)
- Both watermarks created ✅
